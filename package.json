{
  "name": "draw",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run vite\" \"npm run tailwind\"",
    "vite": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tailwind": "npx tailwindcss -i ./styles/input.css -o ./styles/output.css --watch"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^5.2.0",
    "vitest": "^1.4.0"
  }
}
